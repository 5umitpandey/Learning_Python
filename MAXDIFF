# cook your dish here
t = int(input())

while t:
    n,k = map(int, input().split())
    
    a = []
    a = list(map(int, input().split()))
    
    a.sort()
    
    kid = 0
    for i in range(k):
        kid += a[i]
    
    print(sum(a) - kid - kid)
    
    t -= 1
